# 测试规范

本文档定义项目的测试规范，适用于所有项目。

---

## 1. 测试目的

代码完成后，通过测试验证：
1. 代码实现是否符合设计文档
2. 功能是否满足需求文档
3. 边界条件和异常处理是否正确

---

## 2. 测试目录结构

```
project/
├── tests/                      # 测试目录
│   ├── __init__.py
│   ├── conftest.py            # pytest 配置和公共 fixtures
│   ├── fixtures/              # 测试数据
│   │   └── *.json
│   ├── test_*.py            # 测试文件
│   └── report.md             # 测试报告
```

---

## 3. 测试文件编写

### 3.1 conftest.py 基础配置

```python
# -*- coding: utf-8 -*-
"""
pytest配置和公共fixture
"""

import os
import pytest

BASE_DIR = os.path.dirname(os.path.dirname(__file__))


@pytest.fixture(scope='session')
def app():
    """创建测试应用"""
    from apps import create_app
    app = create_app(protect_swagger=False)
    app.config['TESTING'] = True
    return app


@pytest.fixture(scope='session')
def client(app):
    """创建测试客户端"""
    return app.test_client()


@pytest.fixture(scope='function', autouse=True)
def db_session(db, app):
    """每个测试函数独立的数据库会话"""
    with app.app_context():
        yield
```

### 3.2 测试类组织

```python
# -*- coding: utf-8 -*-
"""
认证接口测试
"""

import pytest


class TestAuthLogin:
    """登录测试"""

    def test_login_success(self, client):
        """测试登录成功"""
        response = client.post('/{项目前缀}/auth/login',
                              json={"username": "admin", "password": "xxx"})
        assert response.status_code == 200
        data = response.get_json()
        assert data['code'] == 0
        assert 'token' in data['data']
```

### 3.3 测试数据 fixtures

```json
// tests/fixtures/auth.json
{
    "login": {
        "success": {
            "username": "admin",
            "password": "0192023a7bbd73250516f069df18b500"
        }
    }
}
```

---

## 4. 测试用例来源

测试用例应覆盖需求文档中的：

| 需求类型 | 测试点 |
|:---------|:-------|
| 接口列表 | 每个接口是否实现 |
| 参数说明 | 必填参数、选填参数、边界值 |
| 响应格式 | 成功响应、失败响应 |
| 业务逻辑 | 状态流转、条件判断 |

---

## 5. 测试执行

```bash
# 运行所有测试
pytest tests/ -v

# 运行指定文件
pytest tests/test_auth.py -v

# 生成覆盖率报告
pytest tests/ --cov=. --cov-report=html
```

---

## 6. 测试报告（强制）

测试完成后，必须生成测试报告。

### 6.1 报告位置

```
project/tests/report.md
```

### 6.2 报告内容

```markdown
# 测试报告

| 项目 | 内容 |
|:-----|:-----|
| 项目名称 | xxx |
| 测试日期 | 2026-05-12 |
| 测试结果 | 通过/未通过 |
| 测试人员 | xxx |

---

## 一、需求覆盖情况

| 需求编号 | 需求描述 | 对应功能/接口 | 测试结果 |
|:---------|:---------|:--------------|:---------|
| REQ-001 | 用户名密码登录 | POST /{项目前缀}/auth/login | ✅ 通过 |
| REQ-002 | 用户登出 | POST /{项目前缀}/auth/logout | ✅ 通过 |
| REQ-003 | 查询用户列表 | GET /{项目前缀}/user/list | ✅ 通过 |

---

## 二、功能测试明细

### 2.1 用户登录功能

#### 2.1.1 参数校验

| 参数 | 校验项 | 测试值 | 预期行为 | 实际结果 | 状态 |
|:-----|:-------|:-------|:---------|:---------|:-----|
| username | 必填 | 空 | 提示必填 | 提示必填 | ✅ |
| username | 长度 | 1字符 | 提示长度不足 | 提示长度不足 | ✅ |
| username | 格式 | 邮箱格式 | 提示格式错误 | 提示格式错误 | ✅ |
| password | 必填 | 空 | 提示必填 | 提示必填 | ✅ |
| password | 长度 | 6位 | 提示密码太短 | 提示密码太短 | ✅ |

#### 2.1.2 业务逻辑

| 测试场景 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|:---------|:---------|:---------|:---------|:-----|
| 正常登录 | 用户名密码正确 | 返回token | 返回token | ✅ |
| 密码错误 | 密码错误3次 | 账号锁定 | 账号锁定 | ✅ |
| 用户不存在 | 用户名不存在 | 提示用户不存在 | 提示用户不存在 | ✅ |
| token过期 | token已过期 | 提示重新登录 | 提示重新登录 | ✅ |

#### 2.1.3 异常处理

| 异常场景 | 预期行为 | 实际结果 | 状态 |
|:---------|:---------|:---------|:-----|
| 数据库连接失败 | 提示服务异常 | 提示服务异常 | ✅ |
| Redis连接失败 | 提示服务异常 | 提示服务异常 | ✅ |

---

## 三、接口测试明细

### POST /{项目前缀}/auth/login

| 用例编号 | 用例描述 | 输入 | 预期结果 | 实际结果 | 状态 |
|:---------|:---------|:-----|:---------|:---------|:-----|
| AUTH-001 | 正常登录 | admin/123456 | code=0,返回token | code=0,返回token | ✅ |
| AUTH-002 | 密码错误 | admin/wrong | code=10005 | code=10005 | ✅ |
| AUTH-003 | 用户不存在 | notexist/123456 | code=10001 | code=10001 | ✅ |
| AUTH-004 | 缺少用户名 | /123456 | code=400 | code=400 | ✅ |
| AUTH-005 | 缺少密码 | admin/ | code=400 | code=400 | ✅ |

---

## 四、测试结果汇总

| 类别 | 总数 | 通过 | 失败 | 通过率 |
|:-----|:-----|:-----|:-----|:-------|
| 参数校验 | 10 | 10 | 0 | 100% |
| 业务逻辑 | 8 | 8 | 0 | 100% |
| 异常处理 | 5 | 5 | 0 | 100% |
| 接口测试 | 20 | 20 | 0 | 100% |

---

## 五、未通过项

| 用例 | 问题描述 | 严重程度 | 备注 |
|:-----|:---------|:---------|:-----|
| - | 无 | - | - |

---

## 六、逻辑合理性评估

| 评估项 | 结论 | 说明 |
|:--------|:-----|:-----|
| 登录失败次数限制 | ✅ 合理 | 失败5次锁定15分钟 |
| token有效期 | ✅ 合理 | 24小时 |
| 密码加密方式 | ✅ 合理 | MD5 |
```

### 6.3 报告要求

| 要求 | 说明 |
|:-----|:-----|
| 需求覆盖 | 每个需求点都必须有对应测试 |
| 接口覆盖 | 每个接口都必须测试 |
| 结果记录 | 必须记录实际测试结果 |
| 问题记录 | 未通过的用例必须详细记录 |

---

## 7. 测试原则

| 原则 | 说明 |
|:-----|:-----|
| 独立性 | 每个测试函数独立运行，不依赖其他测试 |
| 可重复 | 测试可以多次运行，结果一致 |
| 自包含 | 测试数据在 fixtures 中管理 |
| 明确断言 | 每个断言都有明确的预期结果 |

---

## 附录

### A. 相关文档

| 文档 | 位置 |
|:-----|:-----|
| API规范 | `docs/技术规范/API规范.md` |
| 需求文档 | `docs/需求文档/` |
| 设计文档 | `docs/设计文档/` |
